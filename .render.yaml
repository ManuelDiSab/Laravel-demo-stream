services:
  - type: web
    name: LAravel-demo-stream
    env: php
    buildCommand: "composer install && php artisan config:cache && php artisan route:cache"
    startCommand: "php artisan serve --host=0.0.0.0 --port=10000"
    plan: free
    region: frankfurt
    autoDeploy: true
